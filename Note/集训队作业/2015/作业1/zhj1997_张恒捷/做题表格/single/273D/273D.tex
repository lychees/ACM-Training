\section{273D Dima and Figure}
\subsection*{试题大意}
Dima喜欢在一块长方形纸片上作自己喜欢的画。

一块大小为N*M的长方形纸片，包含着N行与M列，并且初始所有格子都是白色的。

Dima每一次作画，会把纸片上一些格子涂黑，并定义这幅“画”就是所涂黑的格子。

Dima喜欢一幅画仅当：

*包含至少一个涂黑的格子。

*所有的涂黑的格子形成一个连通块，换句话说，你可以从任意一个涂黑的格子移动到另一个任意涂黑的格子（一个格子可以移动到四联通的格子里）。

*从一个涂黑的格子(x1,y1)到另一个涂黑的格子(x2,y2)所需的最少移动步数等于$|x1-x2|+|y1-y2|$。

请帮助困惑的Dima，在N*M的长方形纸片上，他能画出多少幅自己喜欢的画呢？你只须回答答案对$10^9+7$取模就行了。

$n,m\leq 150$
\subsection*{算法}
由于上述限制，可推得每一行的黑点都是连续的，且每一行的左右边界都是先单调往外，再单调往内的。

用$f[i][j][k][0..1][0..1]$表示做到第i行，第i行的左右端点在j,k,再用0,1记录左右边界是往外还是往内。转移用前缀和可以优化到$O(1)$。

\subsection*{时间复杂度：$O(n*m^2)$，空间复杂度：$O(n*m^2)$}
