\section{241E Flights}
\subsection*{试题大意}
一张n个点m条边的图，你要给每条边定一个1或2的权值，使得1到n所有路径的权值和都相同。

$n\leq 1000,m\leq 5000$
\subsection*{算法}
将那些从1走走不到的和从n反着走走不到的点都去掉（与1到n的路径无关），要1到n所有路径权值和相同，其实要1到每个点都满足才符合要求。设dis[i]为1到i的最短距离，这道题就是个差分约束系统。
\subsection*{时间复杂度：$O(nm)$，空间复杂度：$O(n+m)$}
