\section{253E Printer}
\subsection*{试题大意}
一个打印机，每秒钟打一张纸。某些时刻它会受到一些任务，任务共有n个，每个任务有一个优先级，它要求打印k张纸。任务的优先级互不相同。打印机打纸的时候会选择优先级最高的任务。

现在只有一个任务的优先级无法知道。但是知道了打印机完成它的时刻。求一个合法的优先级。

$n\leq 50000$
\subsection*{算法}
首先，那个任务优先级是可以二分的。

所以，先把可行的优先级列张表出来，共有O(n)个，然后在表内二分位置任务的优先级。知道优先级后，用堆来模拟。

此题还有$O(nlogn)$的复杂度的其他做法。

\subsection*{时间复杂度：$O(nlog^2 n)$，空间复杂度：$O(n)$}
