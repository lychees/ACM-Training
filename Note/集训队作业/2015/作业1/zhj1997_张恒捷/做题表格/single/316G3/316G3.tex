\section{316G3 Good Substrings}
\subsection*{试题大意}
给你一个大串，和n个小串。要你求有多少不同的大串的子串满足它在第i个小串中出现次数在[li,ri]内。

串长l不超过50000，$n<=10$

\subsection*{算法}
用后缀自动机做：

把所有串加上分隔符并起来，加到后缀自动机中，在后缀树中统计这个点的子树中1-n的小串各有几个。在对于那些大串中的点且符合答案的加入答案。

\subsection*{时间复杂度：$O(n^2*50000)$，空间复杂度：$O(n*50000)$}
